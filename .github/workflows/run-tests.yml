name: QGIS Plugin CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      docker:
        image: qgis/qgis:latest
        options: --user root
        ports:
          - 99:99

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Run QGIS Docker container
        run: |
          docker run -d --name qgis-testing-environment \
          -v ${GITHUB_WORKSPACE}:/tests_directory \
          -e DISPLAY=:99 qgis/qgis:latest

      - name: Sleep for Xvfb
        run: sleep 10

      - name: Setup QGIS and enable the plugin
        run: |
          docker exec qgis-testing-environment sh -c "qgis_setup.sh seat"
          docker exec qgis-testing-environment sh -c "rm -f /root/.local/share/QGIS/QGIS3/profiles/default/python/plugins/seat"
          docker exec qgis-testing-environment sh -c "ln -s /tests_directory/ /root/.local/share/QGIS/QGIS3/profiles/default/python/plugins/seat"

      - name: List test directory
        run: docker exec qgis-testing-environment sh -c "ls /tests_directory/tests"

      # - name: Run tests
      # run: docker exec qgis-testing-environment sh -c "export PYTHONPATH=/tests_directory:\$PYTHONPATH && cd /tests_directory && qgis_testrunner.sh tests.test_example"

      # - name: Run tests
      #   run: docker exec qgis-testing-environment sh -c "PYTHONPATH=/tests_directory:\$PYTHONPATH qgis_testrunner.sh tests.test_example"

      # - name: Run tests
      #   run: |
      #     docker exec qgis-testing-environment sh -c "export PYTHONPATH=/tests_directory:\$PYTHONPATH && cd /tests_directory && qgis_testrunner.sh tests.test_example.run_all"

      - name: Run tests
        run: |
          docker exec qgis-testing-environment sh -c "export PYTHONPATH=/tests_directory:\$PYTHONPATH && qgis_testrunner.sh tests.test_example.run_all"
